<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.project.lab.dao.CourseMapperInterface">

  <select id="findById" parameterType="long" resultType="Course">
    SELECT id, title, credits, university_id, opt_lock_version as optLockVersion
    FROM course WHERE id = #{id}
  </select>

  <select id="findAll" resultType="Course">
    SELECT id, title, credits, opt_lock_version as optLockVersion FROM course
  </select>

  <insert id="insert" parameterType="Course" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO course (title, credits, university_id)
    VALUES (#{title}, #{credits}, #{university.id})
  </insert>

</mapper>